% v0_3_theorems.tex
% Framework Theorems (working draft)
%
% This file is intentionally NOT included from main.tex while Paper 3 is frozen at v0.2.
% Use it as a scratchpad for fully-formal theorem statements, dependency auditing, and proof development.
%
% Canonical theorem-workflow protocol (self-governing):
%   - To add ANY new mathematical claim (definition/remark/proposition/lemma/theorem),
%     COPY the canonical template block below and fill it in.
%   - Do not create ad-hoc formats; the template is the interface.
%
% ============================================================================
% CANONICAL CANDIDATE-RESULT TEMPLATE (COPY FROM HERE FOR EVERY NEW CLAIM)
% ============================================================================
%\begin{comment}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Candidate Result
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Status:
%   Candidate
%   Definition
%   Remark
%   Proposition
%   Lemma
%   Theorem
%
% ------------------------------------------------------------
% Existence Gate
% ------------------------------------------------------------
% Q1. Exact statement:
% Q2. Minimal definitions required:
% Q3. Counterexample attempts:
% Q4. Classification (circle one): Definition / Remark / Proposition / Lemma / Theorem
%
% Decision:
%
%\begin{theorem}[Working title] % or definition/remark/proposition/lemma
%\label{...}
%\deps{...}
%...
%\end{theorem}
%
% ------------------------------------------------------------
% Dependency Audit
% ------------------------------------------------------------
% Logical dependencies:
% Interface dependencies:
% Downstream users:
%
% ------------------------------------------------------------
% Proof Development
% ------------------------------------------------------------
% Sketch:
% Missing obligations:
% Failed proof attempts:
%
% ------------------------------------------------------------
% Manuscript Promotion Gate
% ------------------------------------------------------------
% Necessity:
% Independence:
% Narrative role:
% Final decision: Stay internal / Promote to main.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%\end{comment}
% ============================================================================
% END CANONICAL TEMPLATE
% ============================================================================

\section*{Framework Theorems (v0.3 working draft)}

% ------------------------------------------------------------
% Proposition 1: Typed well-formedness (downgraded from planned Theorem 1)
% ------------------------------------------------------------
\begin{proposition}[Typed well-formedness of structural analysis operators]
\label{prop:typed-well-formedness}
\deps{Definitions~\ref{def:observation-spaces} and~\ref{def:structural-analysis-operator}.}
Let $\mathsf A$ be a structural analysis operator. Then there exists an observation space $Y\in\mathsf{Obs}$ such that $\mathsf A: \Sigma\to Y$ is a deterministic total map on canonical structural objects. In particular, for every canonical structural object $\Sigma$, there exists a unique observable value $y\in Y$ with $y=\mathsf A(\Sigma)$. If $\mathsf A$ is evidence-producing, then for every $\Sigma$ there exists a unique pair $(y,e)$ such that $\mathsf A(\Sigma)=(y,e)$ with $y\in Y$ and $e\in\mathsf{Ev}$.

% Classification (Q4): definitional consequence / typing sanity check.
% Rationale: Definition~\ref{def:structural-analysis-operator} already fixes $\mathsf A$ as a deterministic total function $\Sigma\to Y$ for some $Y\in\mathsf{Obs}$.
\end{proposition}

% Pre-proof checkpoint (answer these before attempting any proof sketch):
%
%  (Q1) What exactly is the statement?
%   - Drafted above (typed existence/uniqueness; evidence-producing case included).
%
%  (Q2) Which definitions are necessary and sufficient?
%   - Necessary (for the statement as written):
%       * Def.~\ref{def:observation-spaces} (to quantify $Y\in\mathsf{Obs}$)
%       * Def.~\ref{def:structural-analysis-operator} (to interpret “$\mathsf A$ is a structural analysis operator”)
%       * (If keeping the evidence clause) Def.~\ref{def:witness-object} only for the *type* $\mathsf{Ev}$.
%       * Canonical structural object $\Sigma$ (from Paper~2) as the domain.
%   - Not currently used by this statement (so should not be dependencies unless we strengthen the claim):
%       * Def.~\ref{def:structural-observable}
%       * Def.~\ref{def:representation-invariant-observable}
%       * Def.~\ref{def:perturbation-family}
%   - Sufficiency check: if Def.~\ref{def:structural-analysis-operator} already includes determinism and a total map $\Sigma\to Y$, then the theorem reduces to typed well-formedness.
%
%  (Q3) Could the statement fail for any taxonomy family? (counterexample attempt)
%   - Evidence-producing operators: OK (still functional), unless evidence is intended to be non-unique.
%   - Partial operators: would break existence for some $\Sigma$ (but are excluded if “operator” means total function).
%   - Nondeterministic analyses: would break uniqueness (excluded by “deterministic” in Def.~\ref{def:structural-analysis-operator}).
%   - Probabilistic outputs: would require $Y$ to be, e.g., a distribution space; allowed only if such $Y\in\mathsf{Obs}$ and the operator is still deterministic as a map into that $Y$.
%   - Multi-output analyses: allowed only if product/tuple spaces are in $\mathsf{Obs}$, or the operator is reframed to return a structured $y$.
%   - Transformational analyses returning a new structural object: allowed only if $\Sigma\in\mathsf{Obs}$ (or a wrapper type is), otherwise this is outside the current interface.
%   - Key fork: if Def.~\ref{def:structural-analysis-operator} is intended to permit partiality or nondeterminism, then Q1 must be tightened (or the definition must be revised).
%
%  (Q4) Is this a theorem, or a consequence of the definitions?
%   - Pending Q2/Q3 resolution. Likely a proposition/remark unless “well-definedness” is strengthened beyond typing.
%
% Proof idea (comments only; only after Q1--Q4 pass):
% - If kept as a result, the “proof” should be a short typing argument.
% - If we want a nontrivial theorem, strengthen the conclusion (e.g., well-definedness relative to $\equiv$, or closure/totality conditions that are not definitional).

% ------------------------------------------------------------
% Theorem 2: Representation invariance
% ------------------------------------------------------------
\begin{theorem}[Representation invariance]
\label{thm:representation-invariance}
\deps{Definition~\ref{def:representation-invariant-observable}; the chosen representation-artifact equivalence $\equiv$ from Paper~2; Definition~\ref{def:structural-analysis-operator}.}
Fix an observation space $Y\in\mathsf{Obs}$ and a representation-invariant structural observable $\mathcal O: \Sigma\to Y$ (with respect to $\equiv$). Define a structural analysis operator $\mathsf A$ by $\mathsf A(\Sigma)\coloneqq \mathcal O(\Sigma)$. Then for all canonical structural objects $\Sigma_1,\Sigma_2$,
\[
\Sigma_1\equiv \Sigma_2 \implies \mathsf A(\Sigma_1)=\mathsf A(\Sigma_2).
\]
If $\mathsf A$ is evidence-producing, the invariance requirement applies to the observable value $y$; evidence objects $e$ need not be invariant across representations.
\end{theorem}

% Existence gate (answer these before attempting any proof sketch):
%
%  (Q1) What exactly is the statement?
%   - Drafted above as: “representation-invariant observables induce representation-invariant analysis results.”
%
%  (Q2) Which definitions are necessary and sufficient?
%   - Necessary:
%       * Def.~\ref{def:representation-invariant-observable} (the hypothesis)
%       * Def.~\ref{def:structural-analysis-operator} (to package $\mathcal O$ as an operator $\mathsf A$)
%       * Equivalence relation $\equiv$ from Paper~2
%   - Not used (unless we strengthen the theorem): witness/evidence definition; perturbation families.
%
%  (Q3) Counterexample attempt:
%   - If $\mathsf A$ is not definitionally tied to a representation-invariant observable (i.e., we allow arbitrary operators), the claim can fail.
%   - If we require invariance of evidence $e$ (not just $y$), the claim can fail for standard witnesses (paths, traces) that depend on naming/order.
%
%  (Q4) Theorem vs. definitional consequence?
%   - Risk: as stated, this is essentially a one-line unfolding of Def.~\ref{def:representation-invariant-observable}.
%   - If we want a substantive theorem, strengthen it to cover a broader class of operators (not just $\mathsf A\coloneqq\mathcal O$) or connect to Paper~2 canonicalization guarantees.
%
% Proof idea (comments only; only after Q1--Q4 pass):
% - Unfold the definition of representation invariance and substitute $\mathsf A=\mathcal O$.
% - Keep evidence out of the equality claim; treat evidence via “checkability” only.


% ------------------------------------------------------------
% Issue 1: Canonical Intermediate Boundary
% ------------------------------------------------------------
\subsection*{Canonical Intermediate Boundary}

Composition of structural analysis operators requires an explicit boundary condition at the intermediate codomain.  Let
\[
\mathsf A_1:
\Sigma \to Y_1
\qquad\text{and}\qquad
\mathsf A_2:Y_1\to Y_2
\]
be deterministic structural analysis operators, where $Y_1,Y_2\in\mathsf{Obs}$.  Suppose $Y_1$ is equipped with an intermediate artifact equivalence relation $\equiv_{Y_1}$ identifying representational variants of the same intermediate observation.

\begin{remark}[Intermediate-boundary counterexample]
\label{rem:intermediate-boundary-counterexample}
\deps{Representation equivalence $\equiv$ on canonical structural objects; an intermediate equivalence $\equiv_{Y_1}$ on $Y_1$; Definitions~\ref{def:observation-spaces} and~\ref{def:structural-analysis-operator}.}
If $\mathsf A_1$ maps equivalent inputs to syntactically divergent $Y_1$ artifacts, then composition can break representation invariance even when the initial inputs are equivalent.  Concretely, it may happen that
\[
\Sigma\equiv\Sigma',
\qquad
\mathsf A_1(\Sigma)\not\equiv_{Y_1}\mathsf A_1(\Sigma'),
\]
and that $\mathsf A_2$ is sensitive to this syntactic divergence, so that
\[
\mathsf A_2(\mathsf A_1(\Sigma))\neq
\mathsf A_2(\mathsf A_1(\Sigma')).
\]
Thus a composition theorem cannot rely only on representation invariance at the original structural-object boundary; it must also control the intermediate codomain $Y_1$.
\end{remark}

\begin{definition}[Canonical intermediate boundary]
\label{def:canonical-intermediate-boundary}
\deps{Representation equivalence $\equiv$ on canonical structural objects; intermediate equivalence $\equiv_{Y_1}$ on $Y_1$; Definition~\ref{def:structural-analysis-operator}.}
An operator $\mathsf A_1:\Sigma\to Y_1$ is \emph{representation-invariant into $Y_1$} when, for all canonical structural objects $\Sigma,\Sigma'$,
\[
\Sigma\equiv\Sigma'
\implies
\mathsf A_1(\Sigma)\equiv_{Y_1}\mathsf A_1(\Sigma').
\]
This is the minimal boundary condition required to prevent representation divergence from being introduced before the second operator is applied.
\end{definition}

\begin{lemma}[Composition through a canonical intermediate boundary]
\label{lem:composition-canonical-intermediate-boundary}
\deps{Definition~\ref{def:canonical-intermediate-boundary}; an intermediate equivalence $\equiv_{Y_1}$ on $Y_1$; an output equivalence or equality criterion on $Y_2$.}
Let $\mathsf A_1:\Sigma\to Y_1$ and $\mathsf A_2:Y_1\to Y_2$ be structural analysis operators.  If $\mathsf A_1$ is representation-invariant into $Y_1$ and $\mathsf A_2$ respects equivalence on $Y_1$, i.e.
\[
y\equiv_{Y_1}y'
\implies
\mathsf A_2(y)=\mathsf A_2(y')
\]
(or the corresponding output-equivalence version in $Y_2$), then $\mathsf A_2\circ\mathsf A_1$ is representation-invariant.
\end{lemma}

% Existence Gate notes for Issue 1:
%  - Dependencies:
%      * Representation equivalence $\equiv$ on canonical structural objects.
%      * Intermediate equivalence $\equiv_{Y_1}$ on the codomain of $\mathsf A_1$.
%      * Typed operators $\mathsf A_1:\Sigma\to Y_1$ and $\mathsf A_2:Y_1\to Y_2$.
%      * A statement that $\mathsf A_2$ respects $\equiv_{Y_1}$, with either equality in $Y_2$ or a specified output equivalence on $Y_2$.
%  - Counterexample eliminated:
%      * The syntactic-divergence failure mode is blocked because equivalent structural inputs must land in the same $\equiv_{Y_1}$-class before $\mathsf A_2$ is applied.
%  - Classification risk: theorem vs lemma:
%      * The result is currently a lemma candidate because, once the boundary condition and respectfulness of $\mathsf A_2$ are assumed, the composition claim is a direct relational argument.
%      * It should not be promoted to a theorem until the surrounding framework fixes whether $Y_2$ uses equality or its own output equivalence.
%  - Proof status:
%      * No proof yet.

% ------------------------------------------------------------
% Candidate Theorem: Composition
% ------------------------------------------------------------
\begin{comment}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Candidate Result: Composition
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Status:
%   Candidate
%
% ------------------------------------------------------------
% Existence Gate
% ------------------------------------------------------------
% Q1. Exact statement attempt:
%   Let $Y_1,Y_2\in\mathsf{Obs}$. Let
%   \[
%   \mathsf A_1:\Sigma\to Y_1
%   \qquad\text{and}\qquad
%   \mathsf A_2:Y_1\to Y_2
%   \]
%   be deterministic total maps. If $Y_1$ is an admissible input domain for
%   $\mathsf A_2$ and the composite output space $Y_2$ is admitted as an
%   observation space, then
%   \[
%   \mathsf A_2\circ\mathsf A_1:\Sigma\to Y_2
%   \]
%   is a deterministic total map on canonical structural objects.
%
%   Candidate promotion form, if second-stage maps are admitted as structural
%   analysis operators or as post-observation maps:
%   If $\mathsf A_1$ is an admissible structural analysis into $Y_1$ and
%   $\mathsf A_2$ is an admissible deterministic total post-observation map
%   from $Y_1$ to $Y_2$, then $\mathsf A_2\circ\mathsf A_1$ is an admissible
%   structural analysis into $Y_2$.
%
% Q2. Minimal dependency list:
%   - Canonical structural objects $\Sigma$ as the first-stage domain.
%   - Definition~\ref{def:observation-spaces}, only to require
%     $Y_1,Y_2\in\mathsf{Obs}$.
%   - Definition~\ref{def:structural-analysis-operator}, only for the
%     determinism/totality/admissibility expectations on maps out of $\Sigma$.
%   - A still-unresolved admissibility clause for maps whose domain is an
%     observation space rather than $\Sigma$:
%       * either structural analysis operators are generalized to permit
%         $Y_1\to Y_2$ stages;
%       * or a separate class of deterministic total post-observation maps is
%         admitted;
%       * or the theorem is weakened to a plain typed-composition fact rather
%         than a structural-analysis-operator closure result.
%
% Q3. Counterexample attempts across taxonomy families:
%   - Plain typed maps: if $\mathsf A_1$ and $\mathsf A_2$ are deterministic
%     total maps with matching middle type $Y_1$, composition is typed and
%     total. No additional closure beyond $Y_2\in\mathsf{Obs}$ appears needed.
%   - Structural-analysis-operator taxonomy: if operators are defined only as
%     maps $\Sigma\to Y$, then $\mathsf A_2:Y_1\to Y_2$ is not itself a
%     structural analysis operator unless $Y_1=\Sigma$ or the interface is
%     explicitly extended. The closure risk is therefore admissibility of the
%     second-stage domain, not composition of functions.
%   - Partial analyses: if either stage is partial, the composite can be
%     undefined even when the types match. Totality is required unless partial
%     operators are made explicit.
%   - Nondeterministic analyses: if either stage is nondeterministic, the
%     composite is not a deterministic map without a separate determinization
%     convention. Determinism is required for the current interface.
%   - Evidence-producing analyses: if $\mathsf A_1$ returns $(y_1,e_1)$ while
%     $\mathsf A_2$ expects $y_1$, the middle type does not match. Either the
%     observable projection must be specified, or $\mathsf A_2$ must accept the
%     paired output. No evidence propagation rule is required for the minimal
%     observable-only statement.
%   - Representation-sensitive witnesses: even if observable values compose,
%     evidence objects may fail representation invariance. Therefore any
%     evidence clause should be excluded from the minimal statement unless an
%     admissible evidence interface is separately declared.
%   - Multi-output analyses: if $\mathsf A_1$ returns structured data and
%     $\mathsf A_2$ expects only one component, composition requires an explicit
%     projection or a precisely matching $Y_1$. Do not assume product closure
%     unless the theorem is strengthened to construct paired outputs.
%   - Transformational analyses: if an analysis returns a new structural object
%     and $\mathsf A_2$ is an ordinary structural analysis, composition works
%     only if that returned object type is exactly the canonical structural
%     domain or is explicitly admitted as an observation/input domain. This is
%     another domain-admissibility issue.
%   - Probabilistic-valued analyses: composition is acceptable only when the
%     probabilistic object is treated as an element of $Y_1$ and
%     $\mathsf A_2$ is a deterministic total map on that object. Sampling-based
%     second stages would violate determinism.
%
% Q4. Classification risk: theorem vs proposition:
%   - As a plain statement about deterministic total typed maps, this is at
%     most a proposition or remark: ordinary function composition supplies the
%     result.
%   - It becomes theorem-worthy only if the framework first defines a
%     nontrivial admissibility/closure rule saying when post-observation maps
%     $Y_1\to Y_2$ preserve structural-analysis admissibility.
%   - Current risk: calling it a theorem would overstate a definitional or
%     typing consequence unless the missing admissibility interface is added.
%
% Decision:
%   Keep as Candidate. Do not prove yet. Next action is to determine whether
%   Paper 3 wants (i) a post-observation-map admissibility definition, (ii) a
%   weakened typed-composition proposition, or (iii) no promoted result.
%
% ------------------------------------------------------------
% Dependency Audit
% ------------------------------------------------------------
% Logical dependencies:
%   Canonical structural objects; deterministic total functions; admitted
%   observation spaces; unresolved post-observation admissibility.
% Interface dependencies:
%   Matching middle type $Y_1$; $Y_2\in\mathsf{Obs}$; either admissibility of
%   $Y_1$ as a second-stage input or explicit weakening to plain composition.
% Downstream users:
%   Any later result that chains analyses without re-reading the original
%   structural object.
%
% ------------------------------------------------------------
% Proof Development
% ------------------------------------------------------------
% Sketch:
%   No proof yet.
% Missing obligations:
%   Determine whether the framework admits maps out of observation spaces and
%   whether evidence-producing outputs are excluded, projected, or typed as the
%   actual middle object.
% Failed proof attempts:
%   None yet; proof intentionally deferred.
%
% ------------------------------------------------------------
% Manuscript Promotion Gate
% ------------------------------------------------------------
% Necessity:
%   Pending downstream dependency audit.
% Independence:
%   Pending decision on whether this is more than typed function composition.
% Narrative role:
%   Could justify analysis pipelines only after the admissibility interface is
%   fixed.
% Final decision: Stay internal.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\end{comment}

% ------------------------------------------------------------
% Candidate Theorem: Perturbation Compatibility
% ------------------------------------------------------------
\begin{comment}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Candidate Result: Perturbation Compatibility
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Status:
%   Candidate
%
% ------------------------------------------------------------
% Existence Gate
% ------------------------------------------------------------
% Scope discipline for this gate:
%   Only the already-available interfaces are in scope:
%   \[
%   \delta:\Sigma\to\Sigma,
%   \qquad
%   \mathsf A:\Sigma\to Y,
%   \qquad
%   \mathsf A(\delta(\Sigma)).
%   \]
%   No new definition is introduced here. The gate decides what a future
%   compatibility predicate would have to mean before any proof attempt.
%
% Q1. Exact statement options:
%   Option A -- mere evaluability / typing:
%     If \delta:\Sigma\to\Sigma is an admitted perturbation and
%     \mathsf A:\Sigma\to Y is a structural analysis operator with
%     Y\in\mathsf{Obs}, then the perturbed analysis value
%     \mathsf A(\delta(\Sigma)) is well-typed for every canonical structural
%     object \Sigma. In this reading, "compatibility" means only that
%     perturbations land back in the domain consumed by \mathsf A.
%
%   Option B -- paired counterfactual comparison:
%     If \delta:\Sigma\to\Sigma and \mathsf A:\Sigma\to Y are admitted, then
%     the pair
%     \[
%       (\mathsf A(\Sigma),\mathsf A(\delta(\Sigma)))\in Y\times Y
%     \]
%     is available for a chosen comparison discipline on Y. In this reading,
%     "compatibility" means that original and perturbed observations can be
%     compared without changing the structural-analysis interface. This option
%     requires an already-admitted product/paired observation convention if it
%     is promoted as a formal result; otherwise it should remain explanatory.
%
%   Option C -- relation-preservation / stability:
%     For a specified relation, preorder, metric, or tolerance on Y, a pair
%     (\delta,\mathsf A) is compatible when
%     \mathsf A(\Sigma) and \mathsf A(\delta(\Sigma)) satisfy that specified
%     output-side constraint for all \Sigma in the intended domain of the
%     perturbation. This is the first nontrivial reading, but the output-side
%     relation is not currently part of the minimal interface and must not be
%     silently invented in this candidate.
%
%   Option D -- commutation with an output perturbation:
%     Given a separately specified map \bar\delta:Y\to Y, require
%     \mathsf A(\delta(\Sigma))=\bar\delta(\mathsf A(\Sigma)). This is a strong
%     functorial/commutation reading. It is not available under the present
%     scope because \bar\delta is a new object and many analyses have no
%     natural output-side perturbation.
%
%   Gate preference:
%     The minimal exact statement currently supported is Option A. Option C is
%     the likely substantive future meaning of "compatibility," but only after
%     the manuscript chooses an existing or newly justified output-side
%     comparison structure. Option D should not be the default.
%
% Q2. Minimal dependencies:
%   Necessary for Option A:
%     - Canonical structural objects \Sigma as the shared domain.
%     - Definition~\ref{def:perturbation-family}, only insofar as a
%       perturbation is a deterministic endomap \delta:\Sigma\to\Sigma.
%     - Definition~\ref{def:structural-analysis-operator}, only insofar as
%       \mathsf A is a deterministic total map \Sigma\to Y with
%       Y\in\mathsf{Obs}.
%     - Definition~\ref{def:observation-spaces}, only to type the codomain Y.
%
%   Not necessary for Option A:
%     - Representation invariance.
%     - Evidence/witness structure.
%     - Composition through observation spaces Y_1\to Y_2.
%     - Any perturbation action on Y.
%     - Any metric, preorder, monotonicity, or Lipschitz structure on Y.
%
%   Additional dependencies for stronger options:
%     - Option B needs an admitted way to regard paired values as observations
%       or must be treated as meta-level comparison data.
%     - Option C needs a specified output-side relation/metric/tolerance.
%     - Option D needs an output perturbation \bar\delta:Y\to Y and a reason
%       it is canonical for the chosen analysis.
%
% Q3. Counterexamples across taxonomy families:
%   - Plain evaluability family:
%       No counterexample if \delta is total \Sigma\to\Sigma and \mathsf A is
%       total \Sigma\to Y. This supports Option A as a typing proposition, not
%       as a substantive theorem.
%   - Partial perturbations:
%       If \delta is undefined on some \Sigma, then \mathsf A(\delta(\Sigma))
%       is not guaranteed to exist. Totality of \delta is required.
%   - Non-canonical perturbation outputs:
%       If \delta(\Sigma) leaves the canonical structural-object class, then
%       \mathsf A cannot necessarily consume it. Endomap status is required.
%   - Partial analyses:
%       If \mathsf A is partial, perturbed inputs may fall outside its domain.
%       Totality of \mathsf A is required.
%   - Nondeterministic analyses:
%       If \mathsf A is nondeterministic or sampling-based, the value
%       \mathsf A(\delta(\Sigma)) is not a deterministic observable without a
%       determinization convention. Current structural analysis operators avoid
%       this by definition.
%   - Evidence-producing analyses:
%       The observable value can be evaluated on \delta(\Sigma), but witnesses
%       may change, fail to be representation-invariant, or require different
%       checking data. No evidence compatibility follows from the minimal
%       interface.
%   - Representation-sensitive outputs:
%       Even when \mathsf A(\delta(\Sigma)) is well-typed, equality or
%       invariance between original and perturbed observations can fail.
%       Option A survives; commutation or invariance readings do not.
%   - Monotone / order-valued analyses:
%       A monotonicity claim can fail when the perturbation deletes structure
%       for one observable and adds or rewires structure for another. It needs
%       an explicit order on Y and a perturbation taxonomy that is monotone for
%       that order.
%   - Quantitative / stability analyses:
%       A Lipschitz or bounded-change claim can fail under small structural
%       edits that cause large observable jumps, unless the perturbation size
%       and output metric are specified and the operator is assumed stable.
%   - Set-valued analyses:
%       Inclusion/refinement can fail in either direction depending on whether
%       \delta removes, adds, contracts, or rewires structure. A set relation on
%       Y must be chosen before stating compatibility.
%   - Transformational analyses:
%       If Y itself is a canonical structural-object space, then iteration
%       \mathsf A(\delta(\Sigma)) is still well-typed as an observation, but no
%       claim follows about applying further structural analyses unless the
%       output is admitted as a canonical input.
%   - Probabilistic-valued analyses:
%       If Y is a distribution space and \mathsf A is deterministic into that
%       space, Option A holds. Coupling, stochastic dominance, or sampling-level
%       compatibility would require additional structure.
%
% Q4. Classification (circle one): Definition / Remark / Proposition / Lemma / Theorem
%   Classification decision for the current scope:
%     Proposition or remark, not theorem.
%
%   Rationale:
%     Under the minimal interface, compatibility means domain compatibility:
%     perturbations produce canonical structural objects, and structural
%     analysis operators consume canonical structural objects. That is a direct
%     typing consequence. It is useful because it authorizes the expression
%     \mathsf A(\delta(\Sigma)), but it is not theorem-level unless a
%     nontrivial output-side comparison discipline is added.
%
%   Future theorem threshold:
%     Promote only after selecting a specific compatibility predicate such as
%     stability, monotonicity, refinement, simulation, or commutation with a
%     justified output-side perturbation. Until then, do not prove anything and
%     do not introduce new definitions.
%
% Decision:
%   Keep open as Candidate. Record the minimal supported meaning as typed
%   evaluability of perturbed analyses: \mathsf A(\delta(\Sigma)) is valid
%   because \delta is an endomap on canonical structural objects and \mathsf A
%   is a total map out of canonical structural objects. Treat stronger meanings
%   as taxonomy-specific future options requiring explicit output-side
%   structure.
%
% ------------------------------------------------------------
% Dependency Audit
% ------------------------------------------------------------
% Logical dependencies:
%   Canonical structural objects; deterministic total perturbation endomaps;
%   deterministic total structural analysis operators; admitted observation
%   spaces.
% Interface dependencies:
%   Shared \Sigma boundary between perturbation codomain and analysis domain;
%   no output-side perturbation, metric, preorder, or evidence propagation is
%   assumed.
% Downstream users:
%   Future counterfactual analyses, robustness/sensitivity statements,
%   resilience/redundancy/blast-radius operators, and any result that compares
%   \mathsf A(\Sigma) with \mathsf A(\delta(\Sigma)).
%
% ------------------------------------------------------------
% Proof Development
% ------------------------------------------------------------
% Sketch:
%   No proof yet. The minimal statement would unfold totality and typing only.
% Missing obligations:
%   Choose whether Paper 3 wants a mere typed-evaluability proposition or a
%   substantive taxonomy-specific compatibility predicate on outputs.
% Failed proof attempts:
%   None; proof intentionally deferred until Q1 is narrowed.
%
% ------------------------------------------------------------
% Manuscript Promotion Gate
% ------------------------------------------------------------
% Necessity:
%   Useful as a boundary statement for counterfactual analysis notation, but
%   not necessary if the text can explain typing locally.
% Independence:
%   Minimal Option A is not independent of existing definitions; stronger
%   options would require additional structure.
% Narrative role:
%   Clarifies that perturbation compatibility first means shared-domain
%   evaluability, not commutation or stability by default.
% Final decision: Stay internal.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\end{comment}

% End of file.
